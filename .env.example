# 이 파일을 복사해서 .env.local 로 만들고 실제 값을 채워 넣으세요.
# .env.local 은 절대 GitHub에 커밋하지 마세요 (.gitignore에 이미 포함되어 있음).

# Gemini API 키 — https://aistudio.google.com/apikey 에서 무료로 발급 (카드 등록 불필요)
GEMINI_API_KEY=your_gemini_api_key_here
